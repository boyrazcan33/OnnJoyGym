# Database (Local Development)
spring.datasource.url=jdbc:postgresql://localhost:5432/onjoygym_dev
spring.datasource.username=onjoygym_user
spring.datasource.password=onjoygym123
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Flyway
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true
spring.flyway.locations=classpath:db/migration

# Server
server.port=8080

# Application
spring.application.name=onjoygym-backend

spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.UserDetailsServiceAutoConfiguration

logging.level.org.springframework.web=DEBUG
logging.level.com.onnjoy.backend=DEBUG
